/// @description Check Direction

/*for(var i = 0; i < phy_collision_points; i++)
{
	var ix = phy_collision_x[i], iy = phy_collision_y[i];
	
	//physics_apply_local_impulse(0, 0, nx, ny);
	//effect_create_above(ef_spark, ix, iy, 0, c_gray);
	//effect_create_above(ef_spark, phy_collision_x[i], phy_collision_y[i], 0, c_white);
}*/